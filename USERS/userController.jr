const userModel = require(´../models/userModel´);

const createUser = (req,res) => {
  const newUser = req.boby;
  res.send(`Usuario $(newUser.name} criado com sucesso´);
};

modukke.export = {createUser};
